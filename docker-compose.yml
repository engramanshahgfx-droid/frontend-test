services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tilalr-frontend
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
